apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: microservices-project
  labels:
    app: users-service
spec:
  selector:
    app: users-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002

